# Personal Portfolio Website

A clean, modern, and fully responsive personal portfolio website built using **HTML5** and **CSS3** only. No JavaScript or external frameworks were used.

## 🚀 Features

- **Responsive Design**: Works on Mobile, Tablet, and Desktop.
- **Modern UI**: Dark theme with Navy Blue and Sky Blue color palette.
- **Pure CSS**: Flexbox and Grid layouts, CSS animations, and hover effects.
- **PWA Ready**: Includes `manifest.json` and `offline.html` (requires Service Worker implementation for full functionality).

## 📂 Project Structure

```
portfolio-website/
│
├── index.html          # Home Page
├── about.html          # About Me
├── skills.html         # Skills & Tools
├── projects.html       # Portfolio Projects
├── contact.html        # Contact Form
│
├── css/
│   └── style.css       # Main Stylesheet
│
├── assets/
│   ├── images/         # Project images (empty placeholder)
│   └── icons/          # PWA icons (empty placeholder)
│
├── manifest.json       # PWA Manifest
└── offline.html        # Offline Fallback Page
```

## 🛠️ Setup & Usage

1. **Download** the `portfolio-website.zip` file.
2. **Extract** the contents.
3. Open `index.html` in your web browser.

## 🎨 Customization

- **Colors**: Edit CSS variables in `css/style.css` under `:root`.
- **Content**: Edit the HTML files to update your information.
- **Images**: Place your images in `assets/images/` and update `src` attributes.
- **Icons**: Place `icon-192x192.png` and `icon-512x512.png` in `assets/icons/`.

## 📝 License

Free to use for personal portfolios.
